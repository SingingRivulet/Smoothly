#ifndef SMOOTHLY_SERVER
#define SMOOTHLY_SERVER
#include "controllers.h"
namespace smoothly{
namespace server{

class server:public controllers{
    public:
        server(int thnum);
};

}//////server
}//////smoothly
#endif
